{% extends 'base.html.twig' %}

{% block menu %}
    <div id="items">
        <a href="{{ path('getVrijwilligersPage', { 'page': 'vrijwilligers' }) }}">Algemene informatie</a>
        <a href="{{ path('getVrijwilligersPage', { 'page': 'taken' }) }}">Taken</a>
        <a href="{{ path('getVrijwilligersPage', { 'page': 'vrijwilligersdag' }) }}">Vrijwilligersdag</a>
    </div>
{% endblock %}